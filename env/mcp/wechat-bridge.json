{
  "name": "wechat-bridge",
  "enabled": false,
  "type": "stdio",
  "command": "npx",
  "args": ["-y", "@example/wechat-mcp"],
  "env": {
    "WECHAT_TOKEN": "${wechat.token}"
  },
  "capabilities": {"authority": "external-write", "reversible": false, "data_sensitivity": "secret", "parallel_safe": false, "fallback": "prepare content without sending it", "verification": "read the delivered message identifier from the service"},
  "platforms": ["claude", "codex", "codebuddy"],
  "_comment": "可选 MCP（默认停用）：企业微信 / 个人微信桥接示例。需在 env/secrets.json 增加 \"wechat\": { \"token\": \"...\" }。启用：把 enabled 改为 true 或删除该字段。"
}