{
    "_comment": "Cursor sync. No platform-specific config is required — the renderer (sync/platforms/cursor.py) only writes MCP servers to ~/.cursor/mcp.json via the shared JSON-MCP writer. Skills for Cursor are handled by the registry-based skills surface."
}
