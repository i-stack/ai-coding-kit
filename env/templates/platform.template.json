{
    "_comment": "平台配置模板。复制到 env/platforms/<name>.json，填入该平台配置。敏感值使用 ${platform.field} 占位，同步时从 env/secrets.json 注入。",
    "env": {
        "YOUR_ENV_VAR": "${your-platform.key}",
        "YOUR_BASE_URL": "${your-platform.url}"
    }
}
